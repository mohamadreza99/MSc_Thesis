\chapter{پژوهش‌های پیشین}\label{Chap:Chap2}
\minitoc
\setcounter{footnote}{0}
\pagestyle{plain}
در این فصل ابتدا به مرور کارهای مهم در زمینه یادگیری مستمر پرداخته، سپس پژوهش‌های حائز اهمیت در زمینه متا-یادگیری را معرفی کرده و سپس به کارهای پیشین در زمینه یادگیری متا-مستمر می‌پردازیم.

\section{یادگیری نمونه محدود با داده افزایی}

\section{متایادگیری}

\section{استفاده از 
\trans{بدنه}{Backbone}
\trans{بیانگر}{Expressive}
 برای رسیدن به 
 \trans{کارایی نمونه}{Sample Efficiency}
}

\section{خاصیت مجموعه باز بودن}

برای رویارویی با مجموعه باز بودن دسته‌بندی دو رویکرد اساسی پیشنهاد شده است. یک رویکرد سلبی، دادن
\trans{خودآگاهی}{Self Awareness}
 به مدل است. یعنی یک مدل آگاهی این را داشته باشد که روی چه ورودی می‌تواند خروجی تولید کند. در نتیجه وقتی یک ورودی خارج از
حیطه‌ی توانایی‌های مدل است، از اظهار نظر روی آن خودداری کند. البته داشتن خودآگاهی، گروه بزرگتری از مسائل را در بر می‌گیرد که به سه دسته‌ی عمده تقسیم می‌شوند. گروه اول تشخیص 
\trans{خارج از توزیع}{Out of Distribution}
است. در این گروه تصاویری که از توزیع دادگان آموزش نیستند یا از دامنه‌های دیگر مانند
\trans{حملات تخاصمی}{Adverserial Attacks}
 می‌آیند شناسایی می‌شود
 \cite{Hendrycks2016ABF,Huang2017AdversarialAO}
 .
 گروه دوم طبقه‌بندی
 \trans{واقع‌بینانه}{Realistic}
  است که وقتی یک نمونه برای طبقه‌بندی سخت به نظر می‌رسد، از دسته‌بندی آن خودداری می‌شود
  \cite{Lu2017SafetyNetDA}
  .
  گروه آخر هم مسئله‌ی شناسایی مجموعه باز است که وقتی نمونه‌ای از کلاس بدیع می‌بیند، آن را طبقه‌بندی نکند.
  

اما رویکرد دیگر که به نوعی ایجابی است، در حیطه‌ای است که
مدل، خود را با ذات مجموعه باز بودن مسئله تطبیق دهد و برای تعمیم دانسته‌هایش روی دسته‌های جدید آماده باشد. اگر مدل مولد باشد با تنظیم توزیعی که یادگرفته میتواند به سرعت با اضافه شدن دسته‌های جدید انطباق پیدا کرد. این خاصیت در مدل‌های بدون پارامتر هم نمود دارد. اما در مدل‌های تمایزگذار با اضافه شدن دسته‌های بدیع، نیاز به آموزش مدل از ابتدا بر روی تمامی دسته‌ها است. به همین جهت، در مسائل دسته‌بندی بزرگ مقیاس باید رویکردی را انتخاب کنیم که به کمترین میزان آموزش مجدد شبکه نیاز باشد. البته در مدل‌های تمایزگذار، روش‌هایی پیشنهاد شده است که بتوان وزن های مرتبط با دسته‌های جدید را تنها با طی کردن 
\trans{گذر جلو رو}{Forward Pass}
و بدون آموزش مجدد، بدست آورد.

\subsection{رویکرد سلبی؛ شناسایی دسته‌های بدیع}

در این رویکرد، سعی بر تشخیص دسته‌های بدیع و به نوعی عدم استنتاج مدل روی آن‌ها است. یعنی مدل فرآیندی برای تطابق با دسته‌های جدید و پذیرش آن‌ها طی نمی‌کند. عمده‌ی تمرکز پژوهش‌ها در این دسته روی تشخیص کلاس‌های بدیع در تنظیمات بزرگ مقیاس بوده است. اما در سال‌های اخیر روش‌هایی ارائه شده‌اند که علاوه بر مسائل بزرگ مقیاس با تعداد نمونه کافی، در مسائل نمونه محدود با تعداد دسته‌ی محدود هم عملکرد مناسبی داشته باشند.
در یک روش، به این نتیجه رسیده‌اند که دسته‌بندی مبتنی بر 
\trans{سافت‌مکس}{Softmax}
چون روی دسته‌های آموزش دچار 
\trans{بیش برازش}{Overfit}
است، نمی‌تواند راه حل خوبی برای تشخیص دسته بدیع باشد. به همین دلیل با الهام از رویکرد متایادگیری، در هر وظیفه دسته‌های دیده شده و بدیع را جای داده‌‌اند و علاوه بر تابع هزینه اصلی، یک جمله برای بیشینه کردن آنتروپی توزیع 
\trans{پسین}{Posterior}
برای دسته‌های بدیع قرار داده‌اند
\cite{Liu2020FewShotOR}
.

در یک کار دیگر، ابتدا یک 
\trans{خودکدگذار}{Auto Encoder}
به وسیله‌ی داده‌‌های درون توزیع آموزش داده می‌شود. بر روی این قسمت، یک لایه‌ی دسته‌بند هم اضافه می‌شود تا احتمال تعلق یک نمونه به دسته‌های درون توزیع را بدهد. سپس یک قسمت دیگر به وسیله‌ی داده‌های درون توزیع و دسته‌های بدیع آموزش داده می‌شود که باید تشخیص دهد آیا یک داده از کلاس بدیع هست یا خیر. عملکرد این مدل در شکل 
\ref{fig:openset1}
آمده است
\cite{Hendrycks2016ABF}
.
\begin{figure}[!htbp]
	\begin{center}
		\includegraphics[width=1\textwidth]{images/openset1.png}
%		\vspace{0.5in}
		\caption{
		استفاده از یک خودکدگذار و تولید احتمالات دسته‌بندی برای یک ورودی که بر روی داده‌های درون توزیع آموزش داده شده است (قسمت آبی) و آموزش قسمت قرمز که وظیفه‌ی تشخیص داده‌هایی که از دسته‌های بدیع آمده اند را دارد
			\cite{Hendrycks2016ABF}.
		}	
		\label{fig:openset1}
	\end{center}
\end{figure}


\subsection{رویکرد ایجابی؛ تنظیم مدل با اضافه کردن توانایی تشخیص دسته‌های جدید}

\section{یادگیری سلسله‌مراتبی نمونه محدود}

\section{یادگیری نمونه محدود، بزرگ مقیاس}
%
%\section {کارهای پیشین در زمینه یادگیری مستمر} \label{s2}
%
%در یک دسته‌بندی رایج\cite{Category} و \cite{parisi}، می‌توان کارهای حوزه یادگیری مستمر را به سه دسته زیر تقسیم کرد:
%\begin{enumerate}
%	\item
%	روش‌های مبتنی بر
%	 \textbf{\trans{منظم ‌سازی}{Regularization}}
%	 : در این مجموعه روش‌ها سعی می‌شود تا با اضافه کردن یک سری جمله منظم ساز در تابع هزینه مدل، روند تغییر وزن‌ها را کند کرده و از فراموشی جلوگیری نمایند
%	 \cite{SynapticInt, EWC, VCL}.
%	\item
%	روش‌های مبتنی بر تغییر معماری یا
%	 \textbf{\trans{منزوی سازی وزن‌ها}{Parameter Isolation}}
%	 : در این روش‌ها سعی می‌شود تا از طریق تخصصی سازی وزن‌های شبکه برای وظیفه‌‌های مختلف، از تداخل آن‌ها تا جای ممکن جلوگیری شود. برای برآورده کردن این هدف، یا می‌توان از تکنیک تنک کردن وزن‌های فعال شبکه استفاده کرد \cite{sparse} یا از تکنیک گسترش معماری شبکه
%	 \cite{pnn, DEN, progress, order_rob}.
%	\item
%	روش‌های مبتنی بر 
%	\textbf{\trans{مرور دادگان}{Data Rehearsal}}
%: در این روش‌ها سعی می‌شود تا بخشی از داده‌های آموزشی در یک حافظه جانبی ذخیره شود. از مجموعه دادگان ذخیره شده قبلی می‌توان همراه با دادگان آینده استفاده کرد تا از فراموشی جلوگیری شود
%\cite{Gem, icarl,bilevel,gradient_based, MREGan}.
%	
%\end{enumerate}
%
%\begin{figure}
%	\centering
%	\includegraphics[width=0.9\linewidth]{images/cl_methods}
%	\caption{دسته‌بندی رایج مدل‌های یادگیری مستمر و نحوه قرار گیری هر یک از روش‌ها در این دسته‌ها}
%	\label{fig:clmethods}
%\end{figure}
%
%
%در ادامه از هر یک از سه مورد فوق یک مثال معرفی می‌کنیم:
%
%\subsection{استفاده از جمله منظم‌سازی}
%در این روش هدف آن است که با اضافه کردن یک سری جمله منظم‌ساز به تابع هزینه، از بروز فراموشی در مدل جلوگیری شود. در واقع در این دسته روش‌ها، بعد از استخراج وزن‌های بهینه برای یک وظیفه، انحراف وزن‌ها از نقطه بهینه با جریمه همراه خواهد بود و از این طریق از فراموشی مدل جلوگیری می‌شود
%\cite{SynapticInt, EWC, VCL}
%.
%
%در ادامه این بخش یکی از مقالات مرتبط با این حوزه را بررسی می‌کنیم:
%\subsubsection{
%بررسی مقاله تثبیت کشسانی وزن‌ها (EWC)
%}
%روش 
%\trans{تثبیت کشسانی وزن‌ها}{Elastic Weight Consolidation}
%\cite{EWC}
%جزء کارهای پایه‌ای در زمینه منظم‌سازی به شمار می‌رود و ارجاعات زیادی دارد. برای توضیح این روش، فرض کنید 
%$\theta$
%وزن‌های شبکه باشد و قصد داریم با دید احتمالاتی آموزش وزن‌های شبکه را انجام دهیم. با فرض یک احتمال پیشین روی وزن‌های شبکه، می‌توان تابع هدف بهینه‌سازی را بیشینه کردن 
%$p(\theta \mid S)$
%معرفی کرد. همچنین فعلاً‌ برای سادگی فرض کنید که $S$ فقط از دو مجموعه 
%$S_1$
%و
%$S_2$
%تشکیل شده است. در این صورت با کمک روابط احتمالاتی می‌توان تابع احتمال پسین را به صورت زیر تفکیک نمود:
%
%\begin{equation}\label{eq3}
%	\begin{array}{l}
%		\log p(\theta \mid S) = \log p(\theta \mid S_1, S_2) \propto
%\log p(S_1 , S_2 \mid \theta) + \log ‌p(\theta) \\
%= \log p(S_2 \mid \theta , S_1)+ \log p(S_1 \mid \theta) +\log p(\theta) \\
%= \log p(S_2 \mid \theta)‌+\log p(S_1,\theta) \\
%\propto
%\log p(S_2 \mid \theta) + \log p(\theta \mid S_1)
%	\end{array} 
%\end{equation}
%
%رابطه فوق با این فرض نوشته شده است که توزیع احتمال روی
%$S_2$
%به شرط مشاهده وزن‌های
%$\theta$
%از دادگان 
%$S_1$
%مستقل خواهد بود.
%
%همانطور که مشاهده می‌شود، فرم به دست آمده برای تابع احتمال پسین، یک شکل تدریجی دارد. به عبارت دیگر اگر 
%$p(S_2 \mid \theta)$
%را به شکل یک تابع هزینه برای آموزش وزن‌ها بر اساس 
%$S_2$
%در نظر بگیریم جمله 
%$\log p(\theta \mid S_1)$
%یک جمله منظم ساز خواهد بود که جلوی تغییر وزن‌های شبکه را می‌گیرد. البته مشکل عدم دسترسی به 
%$S_1$
%همچنان وجود دارد. لذا باید برای جمله منظم‌ساز فوق یک تقریب قابل قبول ارائه کنیم.
%
%در مقاله ذکر شده، برای حل این مسئله، توزیع 
%$p(\theta \mid S_1)$
%را یک توزیع گاوسی در نظر می‌گیرد؛ لذا باید تقریب میانگین و واریانس این توزیع گاوسی را مشخص نماید:
%
%
%\begin{itemize}
%	\item[$\circ$]
%	
%	تخمین میانگین:
%	فرض کنید بعد از دیدن مجموعه دادگان 
%	$S_1$
%	وزن‌های
%	$\theta^*_1$
%	به دست آمده است. در این صورت در زمان آموزش مدل بر روی دادگان 
%	$S_2$
%	، 
%	توزیع 
%	$p(\theta \mid S_1)$
%	یک توزیع گاوسی حول
%	$\theta^*_1$
%	در نظر گرفته می‌شود. 
%	
%	\item[$\circ$]
%	واریانس: برای تخمین واریانس در این روش، اولاً‌ فرض می‌شود که ماتریس واریانس یک ماتریس قطری است. ثانیاً‌ برای تخمین واریانس هر بعد، از عکس معیار اطلاعات 
%	\trans{فیشر}{Fisher}
%	 استفاده می‌شود. اطلاعات فیشر ویژگی‌های مطلوبی دارد که برای این کاربرد بسیار مناسب است. از جمله این که می‌توان مقدار اطلاعات فیشر را بر مبنای گرادیان رسیده به هر یک از وزن‌ها محاسبه کرد. به عبارت دیگر اگر بعد از آموزش شبکه روی دادگان
%	$S_1$
%	، در اطراف نقطه بهینه تابع هزینه به گرادیان‌های رسیده به وزن‌های شبکه‌ نگاه کنیم، می‌توان آن را به عنوان معیاری از عکس واریانس‌ها نگاه کرد.
%\end{itemize}
%
%با در نظر گرفتن یک توزیع گاوسی به شکل فوق، در این صورت می‌توان رابطه \ref{eq3} را به شکل زیر بازنویسی کرد:
%\begin{equation}
%	\label{eq5}
%	\begin{split}
%		\mathcal{L}(\theta) = \mathcal{L}_{2}(\theta) + \sum_{i} F_i (\theta_i - \theta_{1,i}^*)
%	\end{split}
%\end{equation}
%
%که منظور از 
%$\mathcal{L(\theta)}$
%تابع هزینه آموزش مدل روی هر دو وظیفه بوده، و منظور از 
%$\mathcal{L}_{2}(\theta)$
%تابع هزینه آموزش مدل روی وظیفه 
%$S_2$
%می‌باشد. همچنین جمله 
%$\sum_{i} F_i (\theta_i - \theta_{1,i}^*)$
%(که در اثر لگاریتم گرفتن از توزیع گاوسی به دست آمد) یک جمله منظم‌ساز به شمار می‌رود. این جمله منظم‌ساز تعبیر شهودی جالبی دارد؛ در واقع تابع هزینه مدل به ازای هر میزان انحرافی که از 
%$\theta_{1}^*$
%بگیرد دچار جریمه می‌شود. اما این که این جریمه چقدر سنگین باشد، کاملاً‌ بستگی به وزن ماتریس 
%$F$
%دارد. بنابراین ایجاد تغییر در وزن‌هایی که در انجام وظیفه اول اهمیت بالایی داشته‌اند، مدل را به شدت جریمه می‌کند در حالی که وزن‌های دیگر این آزادی را دارند تا برای انجام وظیفه‌های دیگر خود را به‌روز کنند. این مثال ما را به یاد تعدادی فنر با نقطه تعادل 
%$\theta_{1}^*$
%می‌اندازد که سختی فنرها با اعضای روی قطر ماتریس 
%$F$
%رابطه مستقیم دارند. به همین دلیل این مقاله نام خود را تثبیت کشسانی وزن‌ها برگزیده است.
%
%از مشکلات این روش می‌توان به تقریبی بودن روابط احتمالاتی اشاره کرد. به عبارت دیگر، تخمین انجام شده برای گاوسی فرض کردن توزیع پسین، تقریبی نادقیق است که در صورت زیاد شدن تعداد وظایف تدریجی، خود را نشان می‌دهد و باعث افت دقت مدل می‌شود.
%
%\subsubsection{ارتباط با ساختار نورون‌های مغزی}
%
%نویسندگان مقاله تثبیت کشسانی وزن‌ها \cite{EWC} ادعا می‌کنند که این ایده را از نورون‌های مغزی موجودات زنده الهام گرفته‌اند و ایده آن‌ها متناظر با یک پدیده زیستی است. طبق مشاهدات صورت گرفته در مغز موش‌ها، بعد از آموزش یک موش برای انجام یک فعالیت خاص، فضاهای محل اتصال برخی نورون‌های این موش‌ها به مرور زمان دچار تغییراتی شده و جسم سیناپسی آن‌ها رشد می‌کند. بعد از تکمیل فرایند یادگیری، سیناپس‌های رشد کرده تثبیت می‌شوند و تا مدت زیادی تغییری نمی‌کنند. همچنین پاک کردن مصنوعی این سیناپس‌ها از طریق راهکارهای زیستی، باعث می‌شود عملکرد موش در آن وظیفه خاص با خطای جدی مواجه شود. از آن‌جایی که تناظر فضای بین سیناپسی با وزن‌های مورد استفاده در شبکه‌های عصبی یک مطلب پذیرفته شده است، پس می‌توان تثبیت شدن سیناپس‌های نورون‌های مغزی را الهام‌بخش ایده مطرح شده در این مقاله دانست.
%
%\subsection{منزوی کردن پارامترها یا تغییر معماری}\label{sec:sparse}
%
%به یاد آورید که ریشه اصلی مشکل در بحث یادگیری مستمر از آن جایی سرچشمه می‌گیرد که مقدار وزن‌های بهینه برای یک وظیفه با وظیفه‌های دیگر در تضاد است و چون در زمان آموزش همه وظیفه‌ها با هم دیده نمی‌شوند، نمی‌توان یک نقطه بینابین مناسب برای ارضای همه وظیفه‌ها پیدا کرد، بلکه هر وزن متناسب با آخرین وظیفه دیده شده تغییر می‌کند و از نقطه بهینه وظیفه‌های قبلی فاصله می‌گیرد.
%لذا در این دسته از روش‌ها، هدف آن است که هر یک از وزن‌های شبکه برای انجام یک وظیفه خاص تخصصی شوند و از این طریق تا جای ممکن از تداخل دو وظیفه جلوگیری شود. 
%در این رویکرد معمولاً‌ دو دسته از روش‌ها جای می‌گیرند. 
%
%دسته اول از روش‌ها سعی می‌کنند تا وزن‌های آموزش دیده شده برای لایه ماقبل آخر یک نمایش تنک از ورودی باشد. با این روش به ازای هر وظیفه خاص تنها تعداد محدودی از نورون‌های لایه ماقبل آخر فعال شده و احتمال این که دو وظیفه متفاوت از یک وزن خاص به صورت مشترک استفاده نمایند بسیار کم می‌شود و از این طریق مشکل فراموشی برطرف خواهد شد\cite{sparse}. برای دریافت شهود بیشتر، می‌توانید به شکل 
%\ref{fig:sparse}
%که در آن یک فضای انتزاعی از وزن‌ها نمایش داده شده است رجوع کنید. در این شکل هر یک از خطوط رنگی کانتورهایی هستند که تابع هزینه هر یک از وظایف روی آن‌ها کمینه می‌شود و خطوط حرکت در این فضا مسیر به‌روزرسانی وزن‌ها را نشان‌ می‌دهند. اگر فضای یادگرفته شده تنک نباشد، با بهینه‌سازی روی هر وظیفه، از محل وزن‌های بهینه برای سایر وظایف دور می‌شویم (شکل سمت چپ) و در صورت تنک بودن، می‌توان با بهینه‌سازی تدریجی روی هر وظیفه به صورت جداگانه، به محلی رسید که برای هر سه وظیفه نقطه‌ای بهینه محسوب می‌شود.
%
%\begin{figure}[h]
%	\centering
%	\includegraphics[width=0.8\linewidth]{images/sparse}
%	\caption{
%	فضای وزن‌های نامناسب برای یادگیری مستمر (سمت چپ) و فضای ایده‌آل با فراموشی اندک (سمت راست)\cite{OML}.
%	در حالت نامناسب، آموزش شبکه برای انجام یک وظیفه باعث می‌شود تا وزن‌های به دست آمده از نقطه مناسب برای وظیفه قبلی فاصله بگیرند. به عبارت دیگر، اگر هر یک از منحنی‌های قرمز، آبی و زرد را نقطه بهینه وزن‌ها برای انجام یک وظیفه در نظر بگیریم، آموزش تدریجی وزن‌ها باعث فاصله گرفتن از منحنی بهینه وظیفه قبلی می‌شود. این در حالیست که در یک فضای تنک (شکل راست) بهینه کردن وزن‌ها در راستای یک وظیفه، تداخلی با نقطه بهینه سایر وظایف ندارد.
%}
%	\label{fig:sparse}
%\end{figure}
%
%
%در دسته دوم از روش‌ها همزمان با دریافت دادگان جدید، بخشی از معماری شبکه دست خوش تغییر می‌شود تا دانش اندوخته شده در گام‌های قبلی حفظ شود.
%برای این منظور معمولاً وزن‌های قبلی که برای وظیفه‌های پیشین مناسب بوده‌اند منجمد شده و وزن‌های جدیدی به مجموعه وزن‌های شبکه افزوده می‌شود. هر چند به نظر می‌رسد که با ثابت شدن وزن‌های پیشین، فراموشی به حداقل مقدار ممکن می‌رسد، اما باید توجه کرد که در این روش‌ها افزایش خطی تعداد وزن‌ها متناسب با تعداد وظیفه‌ها یک مشکل جدی است و از معایب این روش محسوب می‌شود
%\cite{pnn}
%.
%
%در ادامه این بخش، یکی از مقالات متعلق به این دسته را ذکر می‌کنیم:
%
%\subsubsection{
%بررسی مقاله برهم نهی ماسک‌ها (SupInSup)
%}
%در مقاله 
%\trans{برهم نهی ماسک‌ها}{Superposition in Supermasks}
%\cite{supsup}
%از تکنیک ماسک گذاری در شبکه‌های عصبی استفاده می‌شود. برای این منظور فرض کنید که قبل از شروع آموزش، یک شبکه عصبی پایه با وزن‌های تصادفی در اختیار داریم. با دیدن دادگان آموزشی، قصد داریم به جای آموزش خود شبکه، یک ماسک مناسب برای شبکه بسازیم. منظور از ماسک، ضرایب 
%\trans{دودویی}{Binary}
% است که در وزن‌های شبکه ضرب می‌شوند و برخی از وزن‌ها را صفر کرده و برخی دیگر را بدون تغییر ثابت نگه می‌دارند. در مقاله‌ای مرتبط با این حوزه \cite{mask} نشان داده شده است که با استفاده از تکنیک ماسک، حتی اگر هیچ وزنی از شبکه پایه آموزش نبیند، می‌توان تا هر دقت دلخواهی یک مسئله باناظر را حل کرد.
%
%نویسندگان مقاله برهم نهی ماسک‌ها
%\cite{supsup}
%از همین ایده استفاده کرده و به جای آموزش شبکه‌های پایه سعی می‌کنند در هر گام یک ماسک دودویی آموزش دهند. به عبارت دیگر وزن‌های شبکه پایه در این روش ثابت می‌شوند و با دیدن هر وظیفه مثل
%$S_t$
%یک ماسک دودویی متناظر با آن یاد گرفته می‌شود. ماسک یادگرفته شده در گام 
%$t$
%ام برای جداسازی کلاس‌های داخل وظیفه 
%$S_t$
%به خوبی آموزش می‌بیند. بعد از آموزش، ماسک مربوط به هر کلاس در یک حافظه جانبی نگه‌داری می‌شود و در زمان‌های بعدی هیچ تغییری در آن به وجود نمی‌آید
%(به شکل \ref{fig:supinsup} سمت چپ رجوع شود).
%از آن‌جا که شبکه‌ پایه و ماسک مختص هر وظیفه هیچ گونه تغییری نمی‌کنند، بنابراین فراموشی در این روش به حداقل رسیده و وظیفه‌های بعدی نمی‌توانند دانش پیشین را خراب کنند. بدیهی است که علی رغم دودویی بودن ماسک‌ها، ذخیره‌سازی آن‌ها نیاز به حافظه جانبی دارد که به صورت خطی با تعداد وظیفه‌ها رشد می‌کند و این نکته از معایب این روش محسوب می‌شود. 
%
%البته ممکن است این ابهام در اینجا به وجود آید که مدل در زمان تست چگونه می‌فهمد که باید از چه ماسکی استفاده کند. نویسندگان مقاله برای پاسخ به این پرسش دو نکته را ذکر می‌کنند. اول این که در برخی از مسائل یادگیری مستمر، در زمان تست شماره وظیفه داده می‌شود و لذا جستجو فقط به کلاس‌های داخل هر وظیفه محدود می‌شود. به عنوان نکته دوم شرایط عام‌تر که شماره وظیفه در زمان تست از دید مدل مخفی است، می‌توان از معیار آنتروپی به عنوان معیار انتخاب ماسک کمک گرفت. به عبارت دیگر در مواجهه با یک داده در زمان تست، همه ماسک‌های ذخیره شده در حافظه روی شبکه اعمال می‌شود و متناظر با هر یک از این ماسک‌ها یک توزیع احتمال روی شماره کلاس مربوط به آن داده حاصل می‌شود. ماسکی که بیشترین قطعیت را در تولید آن توزیع احتمال داشته باشد (یا معادلاً توزیع احتمال به دست آمده آنتروپی کمتری داشته باشد)، به عنوان ماسک برگزیده انتخاب می‌شود و روی شبکه اعمال می‌شود (به شکل \ref{fig:supinsup} سمت راست رجوع شود).
%
%\begin{figure}[h!]
%	\begin{center}
%		\includegraphics[width=1\textwidth]{images/supinsup.png}
%		\caption{
%			ایده مقاله استفاده از برهم‌نهی ماسک‌ها در یک نگاه. نحوه اعمال ماسک‌های مختلف بر روی وزن‌های شبکه(سمت چپ) . نحوه استنباط شماره ماسک مورد نظر در زمان تست با بیشینه کردن قطعیت (سمت راست).
%			\cite{supsup}.
%		}	
%		\label{fig:supinsup}
%	\end{center}
%\end{figure}
%
%\subsubsection{ارتباط با ساختار نورون‌های مغزی}
%به عنوان نکته پایانی در این بخش، می‌توان به ارتباط تکنیک‌های مطرح شده در این بخش با مغز انسان و ساختار زیستی موجود در آن اشاره کرد. در مغز فرآیند‌هایی وجود دارند که متناسب با روش‌های این حوزه شناخته می‌شوند. در ادامه چند مورد از آن‌ها را اشاره می‌کنیم:
%
%\begin{itemize}
%	\item
%	در مغز انسان فرایندی به نام 
%	\trans{مدولاسیون نورونی}{Neuromodulation}
%	 وجود دارد که شبیه مکانیزم ماسک گذاری عمل می‌کند. هورمون‌هایی که در مغز وجود دارند (مثل
%	  \trans{دوپامین}{Dopamine}
%	  ) در اثر مواجهه با شرایط بیرونی متفاوت شروع به ترشح می‌کنند. این نورون‌ها می‌توانند جمعیت عظیمی از نورون‌های مغزی را خاموش کرده و از فعالیت باز دارند و از این نظر شبیه یک ماسک عمل می‌کنند که بر روی  شبکه نورونی اعمال می‌شود
%	  \cite{eligtrace}
%	  .
%	\item
%	باور قدیمی در مورد مغز آن بوده است که نورون‌های عصبی از سنی به بعد از نظر فیزیکی دیگر رشد و تولید مثلی نخواهند داشت. در مطالعات اخیر سلول‌های بنیادینی در مغز انسان بالغ کشف شده است که قابلیت تولید سلول‌های عصبی جدید را فراهم می‌کند. عده‌ای بر این باورند که این فرایند زیستی می‌تواند الهام بخش توسعه الگوریتم‌های یادگیری مستمر باشد \cite{triplemem} . به عبارت دیگر پیشنهاد آن‌ها افزایش تعداد پارامترهای شبکه مصنوعی در طول زمان به منظور ایجاد ظرفیت جدید برای دریافت تجارب جدید و جلوگیری از بروز تداخل میان تجربه‌های قدیمی‌تر می‌باشد.
%	
%	\item
%	نمایش یادگرفته شده از تصویر ورودی توسط نورون‌های مغزی یک ساختار تنک دارد
%	\cite{sparse}.
%	همانطور که در آغاز این بخش نیز توضیح داده شد، تنک بودن نمایش نورون‌ها باعث می‌شود تا تداخل میان وظیفه‌های مختلف به حداقل برسد. مغز از ویژگی تنک بودن نمایش‌ها هم برای صرفه جویی در مصرف انرژی و احتمالاً برای جلوگیری از تداخل میان تجارب مختلف استفاده می‌کند
%	 \cite{triplemem}.
%	
%\end{itemize}
%
%\subsection{روش‌های مبتنی بر مرور دادگان}\label{sec:data-replay}
%
%ایده استفاده شده در روش‌های مبتنی بر بازپخش دادگان بسیار ساده است: اگر ما اجازه داشته باشیم اندکی از دادگان هر وظیفه را نزد خود نگه‌داریم، چه بهبودی می‌توان به دست آورد؟ 
%یادآوری می‌شود که نگه‌داشتن همه داده‌ها با فرض‌های مسئله یادگیری مستمر در تناقض است. اما نگه‌داشتن بخش کوچکی از داده‌های هر کلاس در این مسئله مجاز است. برای این منظور از یک 
%\trans{حافظه اپیزودیک}{Episodic}
% خارجی استفاده می‌شود. مدل اجازه دارد در هر گام بخش کوچکی از دادگانی که اهمیت دارند را در این حافظه نگه‌داری کند. از ترکیب مجموعه دادگان ذخیره شده به همراه مجموعه دادگان جدیدی که به دست مدل می‌رسد، می‌توان مشترکاً ‌استفاده کرد تا ضمن یادگیری مفاهیم جدید، از فراموشی مفاهیم یادگرفته شده قبلی جلوگیری شود
% \cite{icarl}.
%
%همچنین ایده دیگری که ذیل این بخش مطرح می‌شود آموزش یک مدل 
%\trans{شبکه مولد متخاصمی}{Generative Adversarial Network}
% است که به عنوان جایگزین حافظه جانبی استفاده شود. به عبارت دیگر فرض کنید یک مدل مولد داشته باشیم که یادگرفته تا نمونه‌های مصنوعی از جنس دادگان دیده شده تولید نماید. از این مدل مولد می‌توان کمک گرفت تا در هر گام نقش دادگان ناموجود را برای مدل دسته‌بند شبیه‌سازی کند و از این طریق مشکل فقدان دادگان قبلی احساس نخواهد شد. در بخش بعد، این ایده را به صورت دقیق‌تر با یک مثال شرح می‌دهیم.
%\begin{figure}[h!]
%	\begin{center}
%		\includegraphics[width=1\textwidth]{images/genrep.png}
%		\caption{
%			نمایی از نحوه عملکرد مدل مقاله بازپخش مولدی: نحوه آموزش مدل مولد (چپ) نحوه آموزش مدل دسته‌بند(راست). در زمان آموزش هر وظیفه، ابتدا شبکه مولد یک مجموعه از دادگان مصنوعی به نمایندگی از وظایف قبلی تولید کرده و در کنار داده‌های وظیفه فعلی قرار می‌دهد. در گام بعدی هر دو شبکه مولدی و دسته‌بند روی این مجموعه آموزش می‌بینند.
%			\cite{GenRep}.
%		}	
%		\label{fig:genrep}
%	\end{center}
%\end{figure}
%
%\subsubsection{
%	مقاله بازپخش مولدی (MReGAN)
%}
%
%نویسندگان مقاله 
%\trans{بازپخش مولدی}{Generative Replay}
%\cite{GenRep}
%در کنار شبکه عصبی دسته‌بند خود یک شبکه مولد متخاصمی نیز قرار می‌دهند که این شبکه وظیفه ساخت نمونه‌های مجازی از وظیفه‌های قبلی را بر عهده دارد (رجوع شود به شکل \ref{fig:genrep}). در شروع کار، آموزش شبکه دسته‌بند و شبکه مولد به سادگی توسط 
%$S_1$
%انجام می‌شود. بعد از آموزش شبکه با این مجموعه دادگان، دسترسی مدل به آن‌ها از دست می‌رود و دادگان
%$S_2$
%به مدل تحویل می‌شود. در این گام مدل ابتدا از شبکه مولد خود نمونه‌های مجازی متناسب با وظیفه 
%$S_1$
%تولید می‌کند و آن را 
%$S'_1$
%می‌نامد. سپس این
%$S'_1$
%را به 
%$S_2$
%الحاق می‌کند تا
%$S'=\{S'_1,S_2\}$
%به دست آید. توجه کنید که ساخت 
%$x$
%های مصنوعی داخل 
%$S'_1$
%توسط شبکه مولد انجام می‌شود و استخراج برچسب‌های مصنوعی متناظر با این دادگان توسط شبکه دسته‌بند اتفاق می‌افتد.
%حال مدل می‌تواند آموزش خود را به صورت کامل با 
%$S'$
%انجام دهد و مشکل کمبود دادگان را احساس نخواهد کرد.
%
%بنابراین با این رویکرد مدل قادر است تا در هر گام از دادگان پیشین نمونه‌های مصنوعی تولید نماید و آموزش مدل دسته‌بند خود را توسط این داده‌ها و بدون محدودیت مستمر بودن انجام دهد. 
%
%از مشکلات این روش‌ها می‌توان به این نکته اشاره کرد که با زیاد شدن تعداد وظایف و کلاس‌های مشاهده شده، کیفیت شبکه مولد متخاصمی به مرور افت کرده و از توزیع داده‌های اصلی فاصله می‌گیرد. به عبارت دقیق‌تر هر چند در گام‌های اولیه ممکن است
% $S'_1$
% از 
% $S_1$
% فاصله کمی داشته باشد، اما با طی شدن روند آموزش، کم کم توزیع این دو از هم فاصله گرفته به طوری که در گام‌های نهایی ممکن است بین این دو مجموعه شباهت کمی وجود داشته باشد و در نتیجه مدلی که روی 
% $S'_1$
% خوب کار می‌کند الزاماً‌ دقت خوبی روی 
% $S_1$
% نخواهد داشت.
%
%\subsubsection{ارتباط با ساختار نورون‌های مغزی}
%یکی از قسمت‌های پراهمیت ساختار مغز، بخش 
%\trans{هیپوکمپ}{Hippocampus}
% آن است. در بسیاری از منابع این باور وجود دارد که هیپوکمپ شبیه یک حافظه اپیزودیک و کوتاه مدت عمل می‌کند\cite{fearnet}. در هنگام خواب اطلاعاتی که در هیپوکمپ نگه‌داری می‌شوند برای بخش های دیگر مغز مرور شده و به تدریج در حافظه بلند مدت (
% \trans{قشر نورونی جلوی پیشانی}{Prefrontal Cortex}
% ) تثبیت می‌شود \cite{parisi}. حافظه بلند مدت از جنس دانش چکیده‌ شده‌ای است که از روی تجارب خرد خرد جمع آوری شده است؛ لذا بی‌راه نیست اگر قسمت هیپوکمپ مغز را به منزله یک حافظه یا یک شبکه مولد جانبی تلقی کرده و قشر نورونی مغز را به شبکه عصبی مصنوعی دسته‌بند تشبیه کنیم.
%
%
%\begin{figure}[h]
%	\centering
%	\includegraphics[width=0.7\linewidth]{images/brain}
%	\caption{
%		ساختار مغز و محل قرار گیری هیپوکمپ و قشر نورونی جلوی پیشانی
%		\cite{brain_hipo}.
%		محققین علوم اعصاب معتقدند که تعامل میان این دو بخش نقش خیلی مهمی در یادگیری انسان دارند.
%	}
%	\label{fig:brain}
%\end{figure}
%
%
%\section{متا-یادگیری}\label{s3}
%
%ما تا اینجا روش‌های مرسومی که برای حل مسئله یادگیری مستمر مطرح شده بود را بررسی کردیم. اخیراً‌ تکنیک دیگری برای حل این مسئله با یک رویکرد جدید مطرح شد که در این قسمت می‌خواهیم به آن اشاره نماییم\cite{OML}. برای این منظور، ابتدا متا-یادگیری را به صورت خلاصه معرفی می‌کنیم. 
%
%متا-یادگیری ابتدا با هدف حل مسئله آموزش با تعداد نمونه کم  معرفی شد، اما به مرور زمان توانست در بسیاری دیگر از حوزه‌ها نیز وارد شود. به صورت خلاصه متا-یادگیری را به این صورت تعریف می‌کنند : «یادگرفتن این که چگونه خوب یاد بگیریم!». فرض کنید که مدل ما به دنبال حل یک مسئله یادگیری می‌باشد و آن را با $\mathcal{T}$ نمایش می‌دهیم. اما در این که خود این مسئله یادگیری چگونه حل شود، قابلیت یادگیری وجود دارد. بنابراین اولین چیزی که در این تعریف با آن روبه‌رو می‌شویم، دسته‌ای از مسائل یادگیری است که آن را
%$\mathcal{D}_{meta} $
%می‌ نامیم:
%\begin{equation}
%	\begin{split}
%	\mathcal{D}_{meta} = \{\mathcal{T}^{(1)}, \mathcal{T}^{(2)} , ..., \mathcal{T}^{(n)}\}
%	\end{split}
%\end{equation}
%مدل سعی می‌کند از روی این دسته مسائل، نحوه درست یادگرفتن را خوب یاد بگیرد. 
%هر کدام از این مسائل یادگیری خود از دو بخش مجموعه دادگان 
%\trans{پشتیبان}{Support} 
%$S$
% و 
%\trans{پرسمان}{Query}
%$Q$
%  تشکیل شده است. از مجموعه دادگان پشتیبان برای آموزش و از پرسمان برای ارزیابی استفاده می‌شود:
%\begin{equation}
%	\begin{split}
%\mathcal{T}^{(i)} = \{S^{(i)},Q^{(i)}\}
%	\end{split}
%\end{equation}
%
%در هر گام متا-یادگیری، یک مسئله مثل $\mathcal{T}$ از 
%$\mathcal{D}_{meta}$
%انتخاب می‌کنیم؛ مدل را روی آن آموزش می‌دهیم و بسته به این که چقدر خوب عمل کرده باشد از تابع هزینه استفاده کرده و بخشی از پارامتر‌های مدل را به‌روز می‌کنیم. 
%
%پس در روش‌های متا، یادگیری در دو سطح انجام می‌شود و هر سطح پارامترهای مخصوص خود را دارد؛ پارامترهای 
%$\theta$
%پارامترهای کلی مدل هستند که برای تمام مسائل داخل
%$\mathcal{D}_{meta}$
%مشترک هستند و به آن‌ها 
%\trans{متا-پارامتر}{Meta-Parameters}
%نیز گفته می‌شود. پارامترهای 
%$\phi$
%
%\trans{پارامتر‌های مختص مسئله}{Task-Specific Parameters}
% هستند که از روی 
%$S^{(i)}$
%و با کمک $\theta$ آموزش داده می‌شوند. بعد از تکمیل آموزش $\phi$، با توجه به عملکرد مدل روی $Q^{(i)}$، پارامترهای $\theta$ به‌روز می‌شوند و آموزش 
%$\theta$
%از این طریق اتفاق می‌افتد.
%دائماً‌ با تکرار این چرخه، $\theta$ مقدار مناسبی می‌گیرد به طوری که در مواجهه با یک $\mathcal{D}$ جدید، استخراج $\phi$ از روی 
%$S_i$
%خیلی سریع و بهینه می‌تواند انجام شود. توجه کنید که مسیر آموزش $\theta$ از 
%$\phi$
%می‌گذرد. به عبارت دیگر $\theta$ای مطلوب تر است که بتواند با دیدن یک $S^{(i)}$،
%$\phi$
%بهتری پیشنهاد دهد.
%
%پس به طور خلاصه روش‌های متا-یادگیری از دو فاز تشکیل شده‌اند. در فاز 
%\trans{متا-آموزش}{Meta-Train}
%، مدل به دنبال جمع آوری یک دانش پیشین از روی مسائل نمونه‌ای است که داخل
%$\mathcal{D}_{meta}$
%قرار دارد. این دانش پیشین در $\theta$ ذخیره می‌شود.
%بعد از جمع آوری این دانش پیشین و تکمیل مراحل آموزش، حال نوبت به فاز 
%\trans{متا-ارزیابی}{Meta-Test}
% می‌رسد و ارزیابی مدل صورت می‌پذیرد. توجه کنید که ارزیابی با توجه به مجموعه دادگانی انجام می‌شود که در
%$\mathcal{D}_{meta}$
%دیده نشده است (یعنی  $\mathcal{T} \notin \mathcal{D}_{meta}$). در ادامه این بخش، ابتدا نمادگذاری و ادبیات متا-یادگیری را به صورت دقیق‌تر معرفی نموده و سپس به صورت مختصر سه دسته از روش‌های متا-یادگیری را معرفی خواهیم کرد.
%
%\subsection{ساختار کلی روش‌ها} 
%همانطور که پیش از این نیز ذکر شد، برای حل مسئله با استفاده از متا-یادگیری، مجموعه‌ی پارامتر‌های مدل به دو دسته تقسیم می‌شوند. یکی مجموعه‌ی 
% $\theta$ 
%و دیگری مجموعه‌ی 
%$\phi$, 
%که مجموعه‌ی اول نماینده‌ی تجربه‌ی کسب شده از حل کردن مسائل مختلف بوده و دیگری برای حل کردن هر مسئله به طور جداگانه یادگرفته می‌شود. اصطلاحاً به مدلی که با یادگرفتن پارامتر‌های مختص مسئله یک مسئله‌ی خاص را حل می‌کند 
%\trans{یادگیر پایه}{Base Learner} 
%$\mathcal{B}$
% می‌گوییم که با دسترسی داشتن به مجموعه دادگان یک مسئله‌ی خاص و مقدار متا-پارامتر‌ها، مقدار پارامتر‌های مختص هر مسئله را برای کمینه کردن تابع هزینه تخمین می‌زند.
%
%\begin{eqnarray}
%	\phi = \mathcal{B}\left(\mathcal{S} ; \theta \right)=\underset{\phi}{\arg \min } \ \mathcal{L}^{{base}}\left(\mathcal{S} ; \theta, \phi\right)
%\end{eqnarray} 
%
%که در آن $\mathcal{L}^{{base}}$ تابع هزینه‌ی مختص مسئله می‌باشد. 
%با در دست داشتن یادگیر پایه، مدل دیگری به نام 
%\trans{متا-یادگیر}{Meta-Learner}
%  $\mathcal{M}$ 
%درنظر می‌گیریم که با استفاده از مجموعه مسائل
%$\mathcal{D}_{meta}$
% به دنبال پیدا کردن مقدار پارامتر‌های مختص مسئله است، به گونه‌ای که یادگیر پایه با استفاده از آن‌ها بتواند مسائل را بهتر حل کند.
%\begin{equation}
%	\theta^{\star} = \mathcal{M}(\mathcal{D}_{meta})= \underset{\theta}{\arg \min} \  \mathbb{E}_{(\mathcal{S}, \mathcal{Q})\in \mathcal{D}_{meta}}[\mathcal{L}^{{meta }}\left(\mathcal{Q}; \theta, \phi\right), \ where \ \phi=\mathcal{B}(\mathcal{S}; \theta)]
%\end{equation}
%که در آن  $\mathcal{L}^{{meta}}$ تابع هزینه‌ای برای ارزیابی عملکرد متا-پارامتر‌ها می‌باشد که می‌تواند با
%  $\mathcal{L}^{{base}}$
%    هم متفاوت انتخاب شود.
%
%\subsection{روش‌های متداول متا-یادگیری}
%روش‌های مختلف رویکرد‌های متفاوتی برای انتخاب ساختار پارامترهای ذکر شده در قسمت قبل و روش محاسبه آن‌ها  اتخاذ می‌کنند. در این قسمت دسته‌بندی کلی از این رویکرد‌ها ارائه می‌شود:
%
%\subsubsection{
%	روش‌های 
%	\trans{جعبه‌سیاه}{Black-Box}
%}
%این مجموعه از روش‌ها به دنبال یادگرفتن یک مدل به عنوان جعبه‌سیاه هستند که با دریافت توصیفی از یک مسئله، همه یا بخشی از پارامتر‌های هدف را در خروجی تولید کند.  لذا در این روش‌ها فرآیند یادگیری پارامتر‌های مختص هر مسئله به شکل یک جعبه سیاه مدل می‌شود که به طور کامل در فرآیند متا-آموزش یاد گرفته می‌شود.
%
%بخش‌های اساسی تشکیل‌دهنده‌ی این مدل‌ها شامل یک مدل مختص مسئله $g_\phi$ که یک داده ‌را در ورودی دریافت کرده و برچسب آن ‌را در خروجی تخمین می‌زند، یک مدل جعبه‌سیاه $f_\theta$ که تمام نمونه‌های موجود در مجموعه‌ی پشتیبان را دریافت کرده و مقداری متناسب با این داده‌ها برای $\phi$ تخمین می‌زند: 
%
%\begin{eqnarray}
%	\begin{array}{c}
%\phi = \mathcal{B}\left(\mathcal{S} ; \theta \right)= f_\theta(\mathcal{S}) \\
%\hat{y} = g_\phi(x)		
%	\end{array}
%\end{eqnarray}
%
%که 
%$\hat{y}$
%برچسب پیش‌بینی شده برای داده می‌باشد. با توجه به ساختار ذکر شده، مدل $f_\theta$ بایستی قادر به پردازش مجموعه‌ای از داده‌ها باشد که انتخاب‌های رایج شامل 
%\trans{مجموعه‌ عمیق}{Deep Set}
% \cite{DeepSet}، 
%مدل‌های مبتنی بر
%\trans{توجه}{Attention}
%\cite{Attention}, 
%و 
%\trans{شبکه‌های بازگشتی}{Recurrent Neural Networks}
% \cite{LSTM}
% می‌شوند. از مزایای این دیدگاه می‌توان به انعطاف بالای آن‌ها برای انتخاب ساختار پارامتر‌های مختص مسئله اشاره کرد هر چند از چالش‌های اساسی که روش‌های این دسته با آن مواجه هستند زیاد شدن ابعاد پارامترهای $\phi$ می‌باشد که باعث پیچیدگی بیش از حد شبکه جعبه‌سیاه می‌شود و درنتیجه آموزش مناسب آن بسیار سخت و نیازمند داده‌ی بسیار زیاد می‌شود.
%
%\subsubsection{روش‌های بر پایه‌ی بهینه‌سازی}
%در این دسته از روش‌ها پارامتر‌های مختص مسئله با استفاده از فرآیند بهینه‌سازی به دست می‌آیند و مجموعه‌ی متا-پارامتر‌ها معمولا مقدار اولیه‌ای برای این پارامتر‌ها را نشان می‌دهند.
%
%روش MAML \cite{MAML}
%یکی از پایه‌ای ترین روش‌های این دسته است. در این روش با انتخاب یک ساختار دلخواه برای مدل مختص مسئله $g_{\phi}$ مجموعه‌ی متا-پارامترها را به عنوان مقدار اولیه پارامترهای 
%$\phi$
%یاد می‌گیریم. درنتیجه می‌توان برای یادگیر پایه رابطه زیر را نوشت:
%\begin{eqnarray}
%	\phi = \mathcal{B}\left(\mathcal{S} ; \theta \right) = \theta - \alpha \nabla_\theta \mathcal{L}^{{base}}\left(\mathcal{S} ; \theta \right)
%\end{eqnarray}
%که در این رابطه $\alpha$ اندازه‌ی گام بهینه‌سازی را نشان می‌دهد و مقدار $\phi$ فقط با یک گام بهینه‌سازی از $\theta$ به‌دست می‌آید، توجه کنید که در حالت کلی می‌توان بیش از یک گام بهینه‌سازی برای به‌دست آوردن $\alpha$ استفاده کرد. 
%
%از مزایای این روش‌ها می‌توان به توانایی آن‌ها در حل مسائلی که فاصله بیشتری از مسائل دیده شده در متا-ترین دارند اشاره کرد، همچنین روش‌های خانواده‌ی MAML درصورتی که شبکه مناسبی برای مدل انتخاب شود قابلیت یادگیری توابع پیچیده را دارند. اما نیاز به بهینه‌سازی درجه دوم یکی از مشکلات مهم این دسته از روش‌ها می‌باشد که باعث افزایش پیچیدگی فرآیند یادگیری و طولانی شدن زمان آموزش و ارزیابی می‌شود.
%
%\subsubsection{
%روش‌های مبتنی بر یادگیری متریک
%} \label{sec:metric}
%با توجه به پیچیدگی‌های دو دسته‌ی قبل در تخمین پارامتر‌های مختص مسئله، دسته‌ای از روش‌ها معرفی شد که مدل مختص مسئله‌ی آن‌ها مدلی مبتنی بر مقایسه فاصله در فضای نهان و 
%\trans{یادگیری متریک}{Metric Learning}
% می‌باشد. اولین تصمیم برای رسیدن به یک روش از این دسته، انتخاب یک تابع دسته‌بند مناسب می‌باشد، که اکثر روش‌های این دسته به‌دنبال اولین روش مبتنی بر مفهوم 
%\trans{نماینده دسته}{Class Prototype}
%\cite{protonet}
%از دسته‌بندهای بر پایه‌ی مقایسه استفاده می‌کنند. در این روش‌ها از متا-پارامتر‌ها برای مدل‌سازی یک تابع استخراج ویژگی $f_\theta$ استفاده می‌شود و برای حل هر مسئله با دریافت مجموعه‌ی پشتیبان آن مسئله پس از استخراج ویژگی از نمونه‌های هر دسته، با میانگین‌گیری یک نماینده برای هر دسته تشکیل می‌دهند. به عبارت دقیق‌تر اگر مجموعه همه داده‌هایی که برچسب مشترک 
%$y=j$
%را با مجموعه 
%$S^j = \{x_i\mid(x_i,j) \in S\}$
%نمایش دهیم، نماینده دسته برای این مجموعه به صورت زیر محاسبه می‌شود:
%
%\begin{eqnarray}
%	\label{eqprototype}
%	\begin{array}{c}
%		\mu_j = \frac{1}{\mid S^j \mid} \sum_{i=1}^{\mid S^j \mid} f_{\theta} (x_i) \\
%		\phi = \{\mu_1,\mu_2,...,\mu_N\} 
%	\end{array}
%\end{eqnarray}
%
%این نماینده‌ها به نوعی چکیده دادگان مربوط به یک کلاس هستند. به شرط داشتن یک شبکه استخراج ویژگی مناسب، انتظار می‌رود نماینده دسته به هر یک از دادگان داخل همان کلاس نزدیک بوده و از دادگان کلاس‌های دیگر دور باشد. حال با استفاده از یک تابع مقایسه مناسب، با محاسبه میزان شباهت ویژگی‌های نمونه جدید با نماینده‌ی هر دسته، عملیات دسته‌بندی را انجام می‌دهند. در بسیاری از روش‌ها 
%\cite{protonet}
%از توابع فاصله‌ی ساده‌ای مثل فاصله‌ی اقلیدسی و فاصله‌ی کسینوسی استفاده می‌شود. 
%
%از مزایای این روش‌ها می‌توان به ساده بودن تابع مختص مسئله‌ی آن‌ها اشاره کرد که باعث ساده شدن فرآیند متا-آموزش می‌شود.
%
%
%\subsection{
%	متا-یادگیری از دیدگاه علوم اعصاب
%} \label{sec:meta-learning-neuro}
%
%ایده اصلی متا-یادگیری آن است که پارامتر‌ها را دو سطحی کرده و آموزش را در سطح دنبال کند: سطح متا-پارامتر‌ها یا همان 
%$\theta$
%و سطح پارامتر‌های مختص مسئله یا همان 
%$\phi$.
%به دسته اول پارامتر‌های کند و به دسته‌ دوم پارامتر‌های سریع گفته می‌شود چرا که پارامتر‌های مختص مسئله، به ازای هر مسئله جدید به سرعت تغییر کرده و تلاش می‌کند تا مدل را به مسئله جدید منطبق کند. این در حالیست که پارامتر‌های $\theta$ حاصل تجارب بسیار زیاد است و از حل بسیاری از مسائل قبلی به دست‌ آمده است و اعمال تغییرات در آن به کندی انجام می‌شود. چنین مکانیزمی در مغز جانوارن هوشمند نیز مشاهده می‌شود؛ عده‌ای فرایند یادگیری آهسته وزن‌ها را به تکامل و یادگیری سریع را به تجارب زندگی انسان مرتبط می‌دانند
%\cite{LANGDON202280}
%.
%به عبارت دیگر، این دانشمندان معتقد هستند که سیم کشی‌های اولیه در مغز انسان‌ها در نقطه بهینه‌ای قرار دارد به نحوی که به سرعت با تجارب اندک در طول زندگی، قابلیت یادگیری مسائل موجود در محیط را دارد. به عبارت دیگر، تکامل باعث شده تا این نقطه شروع اولیه برای نورون‌های مغز انسان‌ها فراهم شود. این پدیده خیلی شبیه به دسته روش‌های مبتنی بر بهینه‌سازی به خصوص MAML می‌باشد.
%دسته‌ای دیگر همچنین معتقد هستند که نورون‌های مغز یک انسان به دو دسته سریع و کند تقسیم می‌شوند
%\cite{parisi, BOTVINICK2019408}
%. پارامتر‌های آهسته به کندی در طول زندگی شکل می‌گیرند که این معادل همان متا-یادگیری می‌باشد. شاهد این مدعا آن است که اتصالات نورون‌های مغز انسان (مثلاً‌ در بخش بینایی پس‌سری) به ندرت در اثر دیدن مناظر، گل‌ها یا حیوانات جدید تغییر می‌کنند 
%\cite{Grutzendler2002}
%! این در حالیست که مغز انسان یک مکانیزم سریع نیز برای یادگیری پدیده‌های جدید استفاده می‌کند که این مسئله باعث به وجود آمدن انعطاف در قابلیت یادگیری می‌شود\cite{parisi, BOTVINICK2019408}. لذا به نظر می‌رسد متا-یادگیری نقش مهمی در یادگیری موجودات هوشمند علی الخصوص انسان‌ها دارد و ما قصد داریم تا در این پژوهش از آن در جهت حل مسئله یادگیری مستمر استفاده کنیم.
%
%\section{متا-یادگیری برای حل مسئله یادگیری مستمر}\label{s4}
%\begin{figure}[h!]
%	\begin{center}
%		\includegraphics[width=1.0\textwidth]{images/OML.png}
%		\caption{
%			معماری معرفی شده در مقاله یادگیری متا-مستمر
%			\cite{OML}.
%			در این مقاله، شبکه عصبی به دو بخش استخراج نمایش (آبی) و شبکه دسته‌بند (قرمز) شکسته شده است. وزن‌های شبکه استخراج ویژگی به عنوان متا-پارامتر‌ها در نظر گرفته می‌شوند و در طول متا-یادگیری طوری آموزش داده می‌شود که یک فضای نمایش مناسب برای یادگیری مستمر مهیا شود. وزن‌های مختص هر مسئله که با رنگ قرمز نشان‌ داده شده‌اند، وزن‌های یک شبکه عصبی دو لایه هستند که در طول هر اپیزود از نو یاد گرفته می‌شوند.
%		}
%		\label{fig:OML}
%	\end{center}
%\end{figure}
%
%در بخش \ref{sec:preq} مسائل یادگیری مستمر را معرفی کردیم. حال یک دسته از مسائل یادگیری مستمر را در نظر بگیرید:
%\begin{eqnarray}
%	\mathcal{D}_{meta} = \{ \mathcal{T}^{(1)} , \mathcal{T}^{(2)} , ... , \mathcal{T}^{(n)} \}
%\end{eqnarray}
%
%
%هر کدام از مسائل 
%$\mathcal{T}^{(i)}$
%در تعریف \ref{eq1} صدق می‌کنند و لذا دسترسی آن‌ها به دادگان پشتیبان خود به صورت تدریجی انجام می‌شود:
%
%\begin{eqnarray}
%	\begin{array}{c}
%		\mathcal{T}^{(i)} = (S^{(i)},Q^{(i)})\\
%		S^{(i)} = S_1^{(i)} , S_2^{(i)} , ... , S_n^{(i)}
%	\end{array}
%\end{eqnarray}
%
%ایده‌ای که از متا-یادگیری در اینجا استفاده می‌کنیم بسیار ساده است؛ اگر قبل از این که وارد حل یک مسئله مستمر مثل
%$\mathcal{T}$
%بشویم، روی تعداد زیادی مسئله آزمایشی یادگیری مستمر تمرین کرده باشیم، می‌توان عملکرد بهتری ارائه داد؟ 
%پاسخ مثبت است. نویسندگان مقاله
% \trans{یادگیری متا-مستمر}{Meta-Continual Learning}
% \cite{OML}
%   برای اولین بار استفاده از مفهوم متا-یادگیری برای حل مسئله یادگیری مستمر  را پیشنهاد کردند و روش خود را OML
%   \LTRfootnote{Online aware Meta-Learning}
%    نامیده‌اند. شماتیک عملکرد الگوریتم‌های یادگیری مستمر را می‌توانید به صورت خلاصه در \ref{fig:meta-schematic} مشاهده کنید.
%   
%\begin{figure}[h]
%	\centering
%	\includegraphics[width=1.0\linewidth]{images/meta-schematic}
%	\caption{شماتیک عملکرد الگوریتم‌های یادگیری متا-مستمر.
%نحوه عملکرد خیلی شبیه به فرایند متا-یادگیری عادی است. یعنی در هر اپیزود از متا-یادگیری، ابتدا یک مجموعه پشتیبان پردازش شده و از روی آن پارامترهای مختص مسئله 
%$\phi$
%استخراج می‌شود. در ادامه با توجه به عملکرد این پارامتر	ها روی مجموعه پرسمان، متا-پارامترهای 
%$\theta$
%به‌روزرسانی می‌شوند. این پارامترها منجمد شده و به فاز متا-ارزیابی منتقل می‌شوند و در این فاز با دیدن پشتیبان و پس از استخراج پارامتر‌های مختص مسئله، به پرسمان پاسخ داده می‌شود. تنها تفاوت یادگیری متا-مستمر با متا-یادگیری عادی، دسترسی تدریجی مدل به داده‌های پرسمان داخل هر مسئله است.
%}
%	\label{fig:meta-schematic}
%\end{figure}
%   
%   برای روشن شدن این روش، شکل \ref{fig:OML} را نگاه کنید. در این شکل قسمت آبی رنگ شبکه استخراج ویژگی (یا شبکه تولید 
%\trans{نمایش}{Representation}
%) نام دارد. وزن‌های این شبکه همان متا-پارامتر‌ها یعنی 
%$\theta$
%می‌باشند. این پارامترها بین تمامی مسائل 
%$\mathcal{D}_{meta}$
%مشترک هستند.
%قسمت دوم شبکه که با قرمز نشان داده شده است، همان پارامترهای مختص هر مسئله مستمر (یعنی $\phi$) می‌باشند و برای هر 
%$\mathcal{T}^{(i)}$
%جداگانه استخراج می‌شود.
%بنابراین شبکه عصبی معرفی شده را می‌توان با نماد
%$f_{\theta,\phi}(.)$
%نمایش داد. در واقع یکی از ایده‌های اصلی این مقاله آن بوده است که شبکه عصبی خود را به دو تکه تقسیم کند، یک شبکه استخراج ویژگی و یک شبکه دسته‌بند پارامتری. 
%
%
%\begin{latin}
%	\begin{algorithm}[H]
%		\caption{OML Meta-Train Phase}\label{alg:OMLtrain}
%		\begin{algorithmic}[1]
%			\REQUIRE $\mathcal{D}_{meta}$
%			\STATE randomly initialize $\theta$
%			\WHILE{not done}
%			\STATE randomly initialize $\phi$\;
%			\STATE pick a sample problem $\mathcal{T}=(S,Q)$ from $\mathcal{D}_{meta}$\;
%			\FOR {$\mathcal{S}_t \in \mathcal{S}$}
%			\FOR {$(x_i, y_i) \in \mathcal{S}_t$}
%			\STATE $\phi \leftarrow \phi - {lr}_1 \times \nabla_{\phi}loss(f_{\theta,\phi}(x_i), y_i)$\;		
%			\ENDFOR
%			\ENDFOR	
%			\STATE $\theta \leftarrow \theta - {lr}_2 \times \nabla_{\theta}loss(f_{\theta,\phi}(Q^x), Q^y)$		
%			\ENDWHILE
%			\STATE freeze $\theta$
%			\RETURN $\theta$
%		\end{algorithmic}
%	\end{algorithm}
%\end{latin}
%
%الگوریتمی که برای آموزش در این مقاله پیشنهاد می‌شود، در الگوریتم 
%\ref{alg:OMLtrain}
%نمایش داده شده است. همانطور که پیش‌تر نیز توضیح داده شد، در این الگوریتم یک بهینه‌سازی دو سطحی وجود دارد. توجه کنید که مهمترین تفاوتی که این الگوریتم با یک الگوریتم متا-یادگیری عادی دارد، نحوه دسترسی تدریجی مدل به دادگان موجود در $S$ می‌باشد. به عبارت دیگر اگر به حلقه بهینه‌سازی داخلی نگاه کنیم، یک بهینه سازی تدریجی معمولی می‌بینیم که به شدت در معرض خطر فراموشی قرار دارد. اما آن چیزی که این الگوریتم را ممتاز ساخته است، وجود حلقه بیرونی بهینه‌سازی روی $\theta$ است. مجدداً یادآوری می‌شود که $\theta$ پارامترهای شبکه استخراج نمایش است؛ بنابراین با وجود حلقه بیرونی، مدل یک فضای نمایشی یاد می‌گیرد که در این فضای نمایشی انجام بهینه‌سازی تدریجی با مشکل فراموشی مواجه نشود. در الگوریتم \ref{alg:OMLtest} می‌توان نحوه تست مدل نهایی را مشاهده کرد.
%
%\begin{latin}
%	\begin{algorithm}[H]
%	\caption{OML Meta-Test Phase}\label{alg:OMLtest}
%	\begin{algorithmic}[1]
%		\REQUIRE a continual learning problem: $\mathcal{T}=(S,Q)$, and freezed meta-parameters $\theta$
%		\STATE randomly initialize $\phi$\
%		\FOR {$\mathcal{S}_t \in \mathcal{S}$}
%		\FOR {$(x_i, y_i) \in \mathcal{S}_t$}
%		\STATE $\phi \leftarrow \phi - {lr}_3 \times \nabla_{\phi}loss(f_{\theta,\phi}(x_i), y_i)$\;		
%		\ENDFOR
%		\ENDFOR
%		\STATE $\hat{y} \leftarrow $ predict ($f_{\theta,\phi}(Q^x)$)\
%		\STATE compare $\hat{y}$ to $Q^y$ and return accuracies
%	\end{algorithmic}
%\end{algorithm}
%\end{latin}
%
%پژوهش فعلی ما در ادامه همین مقاله قرار می‌گیرد و قصد داریم تا یادگیری متا-مستمر را بهبود دهیم. لذا در بسیاری از موارد روش خود را با روش OML مقایسه می‌کنیم و سعی می‌کنیم بسیاری از مشکلات موجود در این روش را مرتفع کنیم.
%
%
%به عنوان یک کار پیشین دیگر، که بسیار مرتبط با همین بخش است، مقاله ANML  
%\LTRfootnote{A Neuromodulated Meta-Learning Algorithm}
%را معرفی می‌کنیم\cite{ANML}. در رویکرد اصلی و روش یادگیری این این مقاله نوآوری زیادی وجود ندارد. صرفاً‌ نویسندگان مقاله به این نکته پی‌برده‌اند که با تغییر معماری شبکه استخراج ویژگی می‌توان پیشرفت قابل توجهی به دست آورد. آن‌ها یک شبکه دیگر موازی با شبکه استخراج ویژگی اصلی به کار می‌گیرند که یک ماسک 
%\trans{دودویی}{Binary}
% را برای اعمال روی وزن‌های شبکه اصلی تولید می‌کند. در واقع در این مقاله علاوه بر انجام متا-یادگیری روی شبکه استخراج ویژگی، روی شبکه تولید ماسک (قسمت آبی رنگ در نمودار \ref{fig:anml}) نیز متا-یادگیری را انجام می‌دهد.
%
%\begin{figure}[h]
%	\centering
%	\includegraphics[width=0.8\linewidth]{images/ANML}
%	\caption{ساختار شبکه ANML.
%	وجود یک شبکه مدولاسیونی برای تولید ماسک دودویی قرار گیرنده روی شبکه استخراج ویژگی باعث شده است تا دقت این مدل نسبت به رقیب خود یعنی OML بهتر شود.
%}
%	\label{fig:anml}
%\end{figure}
%
%از آن‌جایی که هسته اصلی متا-یادگیری موجود در این دو مقاله ذکر شده یکسان است، بیش از این به ذکر جزئیات نخواهیم پرداخت. البته توجه کنید که در قسمت آزمایش‌ها هر دو مقاله معرفی شده را بررسی می‌کنیم.
%
%\section{
%	موشکافی فضای نمایش یادگرفته شده در روش OML
%} 
%در این بخش قصد داریم تا کیفیت فضای نمایشی که الگوریتم OML در انتهای متا-آموزش یادگرفته است را به دقت بررسی کرده و علت مؤثر بودن این روش را در آن جا جستجو کنیم. طبق ادعای نوسیندگان OML ، هر چند که در تابع هزینه مستقیماً‌ چنین چیزی از مدل خواسته نشده، اما فضای نمایشی که این مدل یاد می‌گیرد به شدت تنک بوده و از این طریق است که با فراموشی مبارزه می‌کند.
%
%\begin{figure}
%	\centering
%	\includegraphics[width=0.9\linewidth]{images/OMLsparse}
%	\caption{وضعیت فعالیت نورون‌ها در فضای نمایش یادگرفته شده در مدل OML.
%طبق ادعای این مقاله، فضای یادگرفته شده نمایشی داری ویژگی تنک بودن است و از این طریق با فراموشی مبارزه می‌کند. هر چند که این ویژگی به صورت مستقیم در تابع هزینه یا فرایند آموزش لحاظ نشده بود، اما متا-یادگیری باعث شده است تا مدل به چنین فضای نمایشی برسد.	
%}
%	\label{fig:omlsparse}
%\end{figure}
%
%برای روشن شدن بحث به شکل
% \ref{fig:omlsparse}
% توجه کنید. در این شکل، فعالیت‌ نورون‌های آخرین لایه نمایش به تصویر کشیده شده است. همانطور که مشاهده می‌شود، به ازای هر ورودی خاص، تنها تعداد معدودی از نورون‌های این لایه فعال هستند و از سوی دیگر رفتار متوسط نورون‌ها به نحوی است که نشان می‌دهد نورون مرده در این فضای نمایش وجود ندارد. لذا این مشاهده تأیید می‌کند که بهبود به دست آمده توسط مدل OML به دلیل رعایت تنک بودن در فضای نمایش است.
%
%\section{ تاثیر نرخ یادگیری در مدل‌های یادگیری متا-مستمر فعلی
%} \label{sec:lr-problem}
%
%الگوریتم یادگیری متا-مستمر معرفی شده توسط
%OML
%،
% یک الگوریتم متا-یادگیری مبتنی بر بهینه سازی دو سطحی می‌باشد و لذا اگر به الگوریتم \ref{alg:meta-train} توجه کنیم، می‌توانیم حضور دو سطح بهینه سازی و همچنین دو 
%\trans{نرخ یادگیری}{Learning Rate}
%را مشاهده کنیم. اما در الگوریتم متا-ارزیابی
%\ref{alg:OMLtest}
% بهینه سازی فقط در یک سطح انجام می‌شود چرا که متغیرهای 
%$\theta$
%منجمد شده‌اند. لذا در این الگوریتم متوجه حضور یک ابرپارامتر به نام 
%\lr {${lr}_3$}
%می‌شویم. در الگوریتم OML این ابرپارامتر اهمیت فوق العاده زیادی دارد به طوری که زیاد بودن این پارامتر باعث می‌شود تا مدل به سرعت دسته‌های پیشین مشاهده شده را فراموش کند و کم بودن این پارامتر‌ باعث می‌شود تا مدل نتواند به خوبی دسته‌های جدید را یاد بگیرد (رجوع شود به بخش 
%\ref{stabilitytradeoff}
%).
%در کد پیاده‌سازی OML برای تنظیم این ابرپارامتر، الگوریتم متا-ارزیابی را چندین بار روی داده‌های تست با نرخ‌های یادگیری مختلف اجرا می‌کنند و از بین همه آن‌ها نرخی که بیشترین دقت را داشته باشد گزارش می‌کنند. این کار از جنبه‌های متفاوتی ایراد دارد. اولین ایراد اصلی آن است که برای تنظیم این هایپرپارامتر اجازه ندارند از داده‌های تست استفاده نمایند و در بهترین حالت مجاز به استفاده از داده‌های اعتبارسنجی هستند. ایراد دوم آن است که نرخ یادگیری به دست آمده وابسته به شرایط همین تست و تعداد کلاس‌های مشاهده شده می‌باشد. به عبارت دیگر مثلاً اگر ارزیابی روی تعداد 10 کلاس انجام شود، یک نرخ یادگیری بزرگ از طریق این روش استخراج می‌شود در صورتی که اگر روی 600 کلاس ارزیابی انجام شود، در فرایند آزمون و خطا نرخ یادگیری کوچکی به دست می‌آید. این نکته با فرض‌های مسئله یادگیری مستمر در تناقض است چرا که عامل هوشمند در محیط از قبل اطلاع ندارد که چند کلاس قرار است مشاهده نماید. ما در فصل‌های بعد یک روش یادگیری متا-مستمر ارائه خواهیم داد که مبتنی بر یادگیری متریک بوده و در نتیجه این ابرپارامتر مشکل‌زا را از ریشه حذف خواهیم کرد و در بخش‌های بعد به بررسی این مشکل مجدداً باز خواهیم گشت.

\section{جمع‌بندی}

این فصل را معرفی روش‌های یادگیری مستمر آغاز کردیم و در ابتدا سه دسته مبتنی بر بازپخش دادگان، منزوی سازی وزن‌ها و منظم‌سازی را با ذکر یک مثال برای هر یک معرفی نمودیم. در بخش بعد به معرفی روش‌های متا-یادگیری پرداخته و سه زیر روش آن یعنی روش‌های جعبه‌سیاه، روش‌های یادگیری متریک و روش‌های بهینه‌سازی دو سطحی را معرفی کردیم. در ادامه به معرفی روش‌های یادگیری متا-مستمر پرداخته و دو تا از مهمترین کارهای این حوزه را موشکافی کردیم. در فصل بعد قصد داریم تا روش پیشنهادی خود را از ادامه مسیر این دو کار معرفی کنیم.